auf github erstellt

git tag
->lightweight tag : git tag 1.4-lw    Pointer auf ein commit
-> annoted tag wid als objekt in der Datenbank gespeichert: 
Cmd: git tag -a v1.4 -m "my version 1.4"

git push 
